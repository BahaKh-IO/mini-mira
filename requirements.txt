# Verified against the actual code paths this project uses (not against what happened to be
# installed during development -- see notes/deviations.md 1.14 for why termcolor is NOT listed
# here despite coming up while wiring up DINOv3: it's only needed by torch.hub.load's
# hubconf.py route, which mini_mira deliberately bypasses. torchvision and torchmetrics used to
# be excluded for that same reason -- they're back below, but for a different, unrelated need).
#
# torch is pinned, not >=2.0: confirmed directly on the GPU machine that an unpinned torch here
# lets pip silently upgrade it (to satisfy torchvision's own resolution) to a version torchcodec's
# compiled .so files can't load -- "RuntimeError: Could not load libtorchcodec" -- even though the
# files exist and FFmpeg is fine. torch==2.8.0 is real mira's own documented "known-good" pairing
# with torchcodec 0.7.0 (mira/pyproject.toml's decode extra comment).
torch==2.8.0
einops>=0.8
# Used directly by mini_mira.ml.config_loading to parse configs/*.yaml presets (previously only
# a transitive dependency pulled in by other installed packages; 6.0.2 is the version actually
# installed and verified against here).
pyyaml>=6.0
# Used directly by scripts/reconstruct.py to load a real image file (jpg/png/etc.) from disk.
# Lightweight, no heavy dependency chain -- 11.3.0 is the version verified against here.
pillow>=10.0
# Used directly by scripts/reconstruct.py to decode real video files (.mp4/.mov/etc.) frame by
# frame. Checked torchvision.io first -- it dropped read_video/write_video entirely in this
# version (0.28.0), image formats only -- so this is the real, working choice, not a default
# guess. imageio-ffmpeg is imageio's actual video-decoding backend, not optional.
imageio>=2.30
imageio-ffmpeg>=0.6
# Used directly by scripts/reconstruct.py (building comparison-grid arrays) and
# scripts/verify_dataloader.py (a synthetic placeholder frame) -- already an implicit transitive
# dependency of torch/imageio either way, but listed directly since real code imports it by name.
# 2.2.6 is the version verified against here.
numpy>=1.24
# Used directly by mini_mira.codec.loss for the LPIPS perceptual loss term, preferring
# torchmetrics/torchvision metrics over reimplementing from scratch.
# 1.9.0 is the version already installed and verified against here.
torchmetrics>=1.9
# torchmetrics' LPIPS loads its VGG16 backbone through torchvision.models -- confirmed by reading
# torchmetrics' own source (torchmetrics/functional/image/lpips.py), not assumed. A new,
# deliberate need for this one purpose -- distinct from, and not a reversal of, the torch.hub.load
# DINO route this project still avoids (see the comment above torch>=2.0). First real use
# downloads pretrained VGG16 ImageNet weights (~528MB, one-time, needs internet).
#
# Pinned to 0.23.0, not >=0.20: the version PyTorch's own release notes pair with torch==2.8.0
# above. An unpinned/newer torchvision is exactly what pulled in a too-new torch in the first
# place (see the torch==2.8.0 comment) -- these two lines must be changed together, never alone.
torchvision==0.23.0
# Both of these are optional, lazily-imported dependencies of scripts/train_codec.py -- only
# needed if you actually pass --wandb-project / --hf-backup-repo; every other script, and
# train_codec.py itself without those flags, runs fine without either installed (verified
# directly: the offline-mode wandb check and every prior verification run never imported them).
wandb>=0.28          # --wandb-project: loss curves + preview images, in offline mode too.
huggingface_hub>=0.20  # --hf-backup-repo: uploads each checkpoint, for a rented GPU whose disk
                        # may not survive between rentals. Already a transitive dependency via
                        # mira's own dataset.py (scripts/download_shards.py already relies on
                        # it); listed directly now since train_codec.py imports it itself too.
# Needed only for --index-path (real data): mira's own create_loader decodes video through
# torchcodec internally, not imageio -- confirmed directly, not assumed, when wiring this up.
# KNOWN WINDOWS ISSUE: torchcodec needs a "full-shared" FFmpeg build with DLLs; the bundled
# standalone binary imageio-ffmpeg installs doesn't satisfy it (confirmed: it tried and failed to
# load FFmpeg versions 4-8 on this machine). Works on the Linux GPU machine's apt-installed FFmpeg
# 6.1.1 instead (torchcodec supports FFmpeg 4-8) -- see scripts/verify_dataloader.py.
#
# Pinned to 0.7.0 (not left unpinned) for the same reason torch/torchvision above are pinned:
# real mira's own docs call this out as the version that specifically pairs with torch==2.8.0;
# an unpinned torchcodec risks the reverse mismatch (a newer torchcodec built for a newer torch
# than the one pinned above).
torchcodec==0.7.0
# Used directly by mini_mira.world_model.full_eval_metrics for the world-model eval suite's
# Frechet Inception Distance (InceptionV3 + BLOCK_INDEX_BY_DIM) -- matches real mira's own import
# (mira/src/mira/training/metrics/world_model_metrics.py). Not installed on this dev machine as of
# this writing; fill in the real verified version once actually installed on the GPU box.
pytorch-fid
# Used directly by mini_mira.world_model.full_eval_metrics for the world-model eval suite's LPIPS
# metric -- the raw lpips.LPIPS(net="alex") package, distinct from the codec's own
# torchmetrics-based LPIPS (VGG16, mini_mira.codec.loss) -- real mira genuinely uses both for
# different purposes, this isn't accidental duplication. Not installed on this dev machine as of
# this writing.
lpips
# scipy.linalg.sqrtm for the Frechet-distance formula (mini_mira.world_model.full_eval_metrics).
# Already present transitively (1.15.3 on this dev machine, via the torch/torchmetrics chain) but
# listed directly now since new code imports it by name, same reasoning as numpy above.
scipy>=1.11
